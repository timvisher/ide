#!/usr/bin/env bash

cleanup_emacs
tmux kill-server
while [[ $(osascript -e 'application "Ghostty" is running') == true ]]
do
  osascript -e 'tell application "Ghostty" to quit'
  sleep 0.1
done

sleep 0.1

open -a Ghostty
