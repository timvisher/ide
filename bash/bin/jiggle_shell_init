#!/usr/bin/env bash

cleanup_emacs
tmux kill-server
osascript -e 'tell application "Alacritty" to quit'
while [[ $(osascript -e 'application "Alacritty" is running') == true ]]
do
  sleep 0.1
done

sleep 0.1

open -a Alacritty
