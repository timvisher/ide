version: 2
jobs:
  build:
    docker:
      - image: ubuntu:14.04
    steps:
      - checkout
      - run:
          name: Install prerequisites
          command: |
            sudo apt update
            sudo apt-get install --assume-yes nodejs npm shellcheck
      - run:
          name: Run Tests
          command: make -k
