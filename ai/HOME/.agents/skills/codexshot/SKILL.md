---
name: codexshot
description: Run one-shot Codex prompts or reviews via the codexshot CLI. Use when the user mentions codexshot or wants to run a quick Codex prompt outside the current session.
---

# codexshot

Run `codexshot --help` to learn the full command interface, flags, and
usage patterns.
